mixin questions(titleBlock=null, buttons)
    .questionsTemplate
        if titleBlock && titleBlock.type === 'titlepag'
            .mainTitle
                h3= titleBlock.title
        .accordeon
            each button, index in buttons
                .button 
                    input.btnCheck(name="accordeon" id=`accDuvidas-${index}`)
                    label(for=`accDuvidas-${index}`) #{button.titlebtn}
                    .boxText
                        != button.text